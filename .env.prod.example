# Copie para .env.prod e preencha. NUNCA commite o .env.prod.
# Gere segredos com: openssl rand -hex 32

POSTGRES_PASSWORD=
JWT_SECRET=
MASTER_KEY=

# Usuário admin criado na primeira subida (opcional depois disso).
ADMIN_EMAIL=
ADMIN_PASSWORD=

# Só se você usa tradução via Ollama (fora do compose).
# OLLAMA_URL=http://host.docker.internal:11434
# OLLAMA_MODEL=qwen2.5:7b
